<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appix.storemangaementapp">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".OfflineClass"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginSignup.LoginActivity"></activity>
        <activity android:name=".LoginSignup.SignUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TotalStockActivity" />
        <activity android:name=".ShowItems" />
        <activity android:name=".AddActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>